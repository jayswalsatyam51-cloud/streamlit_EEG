# Copy to .env and fill in values. Never commit .env.

# Google Gemini (Streamlit AI analysis)
GEMINI_API_KEY=
# Optional:
# GEMINI_MODEL=gemini-2.5-flash

# DigitalOcean Spaces (API upload — optional if using local Streamlit only)
DO_REGION=
DO_ACCESS_KEY=
DO_ACCESS_SECRET=
DO_BUCKET_NAME=
